{
  "buildCommand": "cd alan && npm run build",
  "outputDirectory": "alan/build",
  "installCommand": "cd alan && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
